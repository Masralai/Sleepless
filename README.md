
Cron-job.org, "Essential because Render's free tier ""sleeps"" n8n after 15 mins. This keeps your daily schedule active."
n8n Container, "Self-hosted in Docker. It processes the logic but ""forgets"" everything on restart except the Workflow JSON."
JSearch API,The Shield,You hit this API to avoid being banned for scraping from a Data Center IP.
Code Node, "Formats messy job descriptions and creates a Unique_ID (e.g., Company+Title+Date)."
Google Sheets, Since Docker on Render has no persistent disk
